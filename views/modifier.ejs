<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
     body {
      font-family: Arial, Helvetica, sans-serif;
    }

    .asterisk-red {
      color: rgb(173, 52, 52);
      font-weight: bold;
    }

    .input-contacts {
      display: flex;
      flex-direction: column;
      box-sizing: border-box;
      width: 85%;
      margin: 1em auto;
    }

    .input-contacts__input {
      background-color: rgb(33, 33, 33);
      background-color: rgba(0, 0, 0, 0.09);
      box-sizing: border-box;
      border: 1px solid grey;
      border-radius: 4px;
      color: #000;
      padding-left: .5em;
      outline-color: silver;
      resize: none;
    }

    .input-contacts__input {
      height: 3em;
    }

    .btn-submit {
      background-color: #0b5351;
      border: none;
      border-radius: 4px;
      box-sizing: border-box;
      color: white;
      font-size: 1.2em;
      height: 3em;
      max-width: 15em;
      width: 85%;
      cursor: pointer;
    }

    .btn-submit:focus {
      outline-color: #bef;
    }

    .btn-submit:hover,
    .btn-submit:active {
      color: #0b5351;
      border: 2px solid #0b5351;
      background-color: transparent;
    }

    /* The Modal (background) */
    .modal {
      /* Hidden by default */
      position: fixed;
      /* Stay in place */
      z-index: 1;
      /* Sit on top */
      left: 0;
      top: 0;
      width: 100%;
      /* Full width */
      height: 100%;
      /* Full height */
      overflow: auto;
      /* Enable scroll if needed */
      background-color: rgb(0, 0, 0);
      /* Fallback color */
      background-color: rgba(0, 0, 0, 0.4);
      /* Black w/ opacity */
      -webkit-animation-name: fadeIn;
      /* Fade in the background */
      -webkit-animation-duration: 0.4s;
      animation-name: fadeIn;
      animation-duration: 0.4s
    }

    /* Modal Content */
    .modal-content {
      position: fixed;
      bottom: 0;
      background-color: #fefefe;
      width: 100%;
      -webkit-animation-name: slideIn;
      -webkit-animation-duration: 0.4s;
      animation-name: slideIn;
      animation-duration: 0.4s
    }

    /* The Close Button */
    .close {
      color: white;
      float: right;
      font-size: 28px;
      font-weight: bold;
    }

    .close:hover,
    .close:focus {
      color: #000;
      text-decoration: none;
      cursor: pointer;
    }

    .modal-header {
      padding: 2px 16px;
      background-color: #5cb85c;
      color: white;
    }

    .modal-body {
      padding: 2px 16px;
    }


    /* Add Animation */
    @-webkit-keyframes slideIn {
      from {
        bottom: -300px;
        opacity: 0
      }

      to {
        bottom: 0;
        opacity: 1
      }
    }

    @keyframes slideIn {
      from {
        bottom: -300px;
        opacity: 0
      }

      to {
        bottom: 0;
        opacity: 1
      }
    }

    @-webkit-keyframes fadeIn {
      from {
        opacity: 0
      }

      to {
        opacity: 1
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0
      }

      to {
        opacity: 1
      }
    }
    #b2n{
      background: rgb(11, 83, 14);
          color: white;
           position: initial;
           padding: 13px;
           border: none;
           border-radius: 0%; width: 20%;
           margin: 3px auto;
    }
</style>
<body>
    <form id="myModal" class="modal" method="POST" action="/Modifier">
        <!-- Modal content -->
        <div class="modal-content">
          <div class="modal-header">
            <span class="close">&times;</span>
            <h2>Reservation</h2>
          </div>
          <div class="modal-body">
            <div class="input-contacts">
                <input placeholder="ID" class="input-contacts__input" id="id" name="id" required type="text" value="<%=list.id%>" readonly>
              </div>
            <div class="input-contacts">
              <input placeholder="Nom" class="input-contacts__input" id="nom" name="nom" required type="text" value="<%=list.nom%>">
            </div>
            <div class="input-contacts">
              <input placeholder="Prenom" class="input-contacts__input" id="prenom" name="prenom" value="<%=list.prenom%>" required type="text">
            </div>

            <div class="input-contacts">
              <input class="input-contacts__input" placeholder="Email" name="email" id="email" value="<%=list.email%>"" required type="email">
            </div>
            <div class="input-contacts">
              <input class="input-contacts__input" placeholder="Telephone" id="telephone" value="<%=list.telephone%>" name="telephone" required type="tel">
            </div>
            <div class="input-contacts">
                <input class="input-contacts__input" placeholder="voiture" id="voiture" value="<%=list2.voiture%>" name="voiture" required type="text">

              </div>
            <div class="input-contacts">
              <label class="input-contacts__title">Date Debut<span class="asterisk-red">*</span>:</label>
              <input class="input-contacts__input" id="date1" name="dateDebut" required type="date">
            </div>
            <div class="input-contacts">
              <label class="input-contacts__title">Date Fin<span class="asterisk-red">*</span>:</label>
              <input class="input-contacts__input" id="date2" name="dateFin" required type="date">
            </div>
          <div class="input-contacts">
          <button class="btn btn-primary" id="b2n">Modifier</button>
        </div>
    </div>
        </form>
</body>
</html>